/// Annotation for local datasource generation
class DartGeneratorLocalDatasource {
  ///
  const DartGeneratorLocalDatasource();
}

/// Annotation for remote datasource generation
class DartGeneratorRemoteDatasource {
  ///
  const DartGeneratorRemoteDatasource();
}
